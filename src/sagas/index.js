// Allows starting sagas in parallel
// eslint-disable-next-line no-unused-vars
import { fork } from 'redux-saga/effects';

// Export root saga
export default function* rootSaga() {
  // Yield and fork sagas here
}
